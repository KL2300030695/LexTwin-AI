fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.9
PyMuPDF==1.24.10
python-docx==1.1.2
networkx==3.3
diff-match-patch==20230430
firebase-admin==6.5.0
anthropic==0.116.0
google-genai==2.11.0

# Chat with Contract (RAG): local embeddings + vector search, not an AI
# provider call -- retrieval is deterministic, only answer synthesis uses
# the configured AI provider (see app/services/chat_service.py). Heavy
# (pulls in PyTorch, ~1.3GB model download on first use).
sentence-transformers==5.6.0
faiss-cpu==1.14.3
pytest==8.3.3
reportlab==4.2.2
python-dotenv==1.0.1
pyarrow==25.0.0  # only needed to run scripts/build_ledgar_playbook.py and build_unfair_tos_playbook.py (reads dataset parquet files); not imported by the running app
