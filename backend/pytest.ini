[pytest]
pythonpath = .
testpaths = tests
addopts = -m "not slow"
markers =
    slow: real-dataset tests that are CPU-heavy (e.g. table extraction on 70+ page PDFs); run explicitly with `pytest -m slow`
