[pytest]
pythonpath = .
testpaths = tests
addopts = -m "not slow"
markers =
    slow: real-dataset / real-local-model tests that are CPU-heavy or download a multi-GB model (e.g. table extraction on 70+ page PDFs, the embedding model, the local LLM); run explicitly with `pytest -m slow`
