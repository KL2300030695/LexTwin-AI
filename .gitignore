# Backend
backend/venv/
backend/__pycache__/
backend/**/__pycache__/
backend/.env
backend/firebase-credentials.json
backend/local_data/
backend/.pytest_cache/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.env

# Raw reference datasets (CUAD, LEDGAR/LexGLUE, ContractNLI -- ~770MB combined,
# re-downloadable, see README Installation step 6). Derived artifacts we
# generate from them (backend/app/playbook/*.json) are still tracked since
# the app needs them at runtime.
/data/

# Claude Code local session/plugin state -- not project content.
.claude/

# Raw demo captures (loose screenshot filenames + a ~193MB video) -- the
# renamed, README-referenced copies live in docs/screenshots/ instead; the
# video is linked from the README via Google Drive rather than committed.
/Demo/

# OS
.DS_Store
Thumbs.db
